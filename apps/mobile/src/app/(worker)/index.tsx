import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function WorkerJobs() {
  return (
    <PlaceholderScreen
      eyebrow="Worker · Jobs"
      title="Assigned jobs"
      description="Live job assignments and accept/decline flows arrive in Phase 3."
    />
  )
}
