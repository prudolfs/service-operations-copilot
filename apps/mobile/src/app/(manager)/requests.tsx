import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function ManagerRequests() {
  return (
    <PlaceholderScreen
      eyebrow="Manager · Requests"
      title="All requests"
      description="Triage, assign, and reassign in Phase 5."
    />
  )
}
