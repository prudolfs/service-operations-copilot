import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function ClientHome() {
  return (
    <PlaceholderScreen
      eyebrow="Client · Home"
      title="Your dashboard"
      description="Submit a service request, track status, and chat with your assigned worker. Live data lands in Phase 3."
    />
  )
}
