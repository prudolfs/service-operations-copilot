# Mirror values from packages/convex/.env.local. Both are public (EXPO_PUBLIC_*)
# because they ship with the bundle; secrets live only on the Convex deployment.
EXPO_PUBLIC_CONVEX_URL=
EXPO_PUBLIC_CONVEX_SITE_URL=
