import { defineSchema } from 'convex/server'

/**
 * Phase 0 placeholder. Real tables (`users`, `serviceRequests`, `chatRooms`,
 * `chatMessages`) come in Phases 1, 3, and 4 respectively.
 */
export default defineSchema({})
